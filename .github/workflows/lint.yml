name: Linting

on:
  push:
    branches:
      - master
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

env:
  NPM_CONFIG_FUND: '0'
  NPM_CONFIG_AUDIT: '0'
  SUPPRESS_SUPPORT: '1'
  NO_UPDATE_NOTIFIER: 'true'

jobs:
  lint:
    name: ESLint Check
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Run code style check
        uses: myrotvorets/composite-actions/node-run-script@c4c07ee643990a78097e4d7b1a1fe481b0f2c3f9 # master
        with:
          script: lint

  typecheck:
    name: TypeScript Check
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Run type check
        uses: myrotvorets/composite-actions/node-run-script@c4c07ee643990a78097e4d7b1a1fe481b0f2c3f9 # master
        with:
          script: typecheck
